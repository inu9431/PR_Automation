services:
  db:
    image: postgres:16-alpine
    container_name: app_db_dev_1
    environment:
      POSTGRES_DB: ${DB_NAME:-dev_db}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

#  redis:
#    image: redis:7-alpine
#    container_name: app_redis_dev
#    ports:
#      - "6379:6379"

  web:
    build:
      context: .
      target: development
    container_name: app_web_dev_1
    volumes:
      - .:/app
    ports:
      - "8003:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/dev_db
    depends_on:
      db:
        condition: service_healthy
    stdin_open: true
    tty: true

volumes:
  postgres_data: